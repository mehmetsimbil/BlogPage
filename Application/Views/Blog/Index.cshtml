@using Entities.Concrete
@model List<Blog>
@{
    Layout = "_Layout";
}

        <table class="table">
            <thead>
            <tr>
                <th scope="col">İD</th>
                <th scope="col">Category İD</th>
                <th scope="col">Title</th>
                <th scope="col">Detail</th>
                <th scope="col">Status</th>
                <th scope="col">Created Time</th>
            </tr>
        </thead>

        <tbody>
            @foreach (var item in Model){
            <tr>

                    <th scope="row" style="color:red">
                   @item.Id
                   </th>
                    <th scope="row" style="color:red">
                        @item.CategoryId
                    </th>
                    <th scope="row" style="color:red">
                        @item.Title
                    </th>
                    <th scope="row" style="color:red">
                        @item.Detail
                    </th>
                    <th scope="row" style="color:red">
                        @item.Status
                    </th>
                    <th scope="row" style="color:red">
                        @item.CreatedTime
                    </th>


            </tr>}
            </tbody>
        </table>



